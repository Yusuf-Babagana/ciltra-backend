name: Deploy Backend to OVH

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: SSH and Update Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: "22"
          script: |
            cd /home/deployer/ciltra-backend
            git pull origin main
            source venv/bin/activate
            pip install -r requirements.txt
            python manage.py migrate --noinput
            python manage.py collectstatic --noinput
            # Use sudo without a password prompt
            sudo systemctl restart gunicorn
            echo "Backend successfully updated!"



script: |
            cd /home/deployer/ciltra-backend
            # 1. Force pull to ignore small local changes if they exist
            git fetch origin main
            git reset --hard origin/main
            
            # 2. Use absolute path for venv
            source /home/deployer/venv/bin/activate
            
            # 3. Standard deployment steps
            pip install -r requirements.txt
            python manage.py migrate --noinput
            
            # 4. Ensure the folder exists before collecting
            mkdir -p staticfiles
            python manage.py collectstatic --noinput
            
            # 5. Restart services
            sudo systemctl restart gunicorn
            echo "Backend successfully updated and Gunicorn restarted!"
